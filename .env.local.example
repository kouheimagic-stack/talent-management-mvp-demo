# MVP 0 demo does not require OpenAI or Supabase.
# Set these only when enabling future AI meeting support.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Set these only when migrating from localStorage mock data to Supabase.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
